
eval "$(starship init bash)"
source <(https-wrench completion bash)
eval "$(zoxide init bash)"
eval "$(direnv hook bash)"
eval "$(doggo completions bash)"

alias cd='z'
alias ..='cd ..'

alias l='ls -alh'
alias l='ls -alh'
alias la='eza -a'
alias ll='eza -lhaa --git --git-repos --no-user --octal-permissions --no-permissions --icons --time-style='\''+%Y-%m-%d %H:%M'\'''
alias lla='eza -la'
alias lldirnewest='llnewest -D'
alias llnewest='eza --sort=time --reverse --oneline --git-ignore --changed --long --no-filesize --no-user --no-permissions --time-style="+%y-%m-%d %h:%m -" --icons=auto'
alias llsize='ll --sort=size --reverse  --total-size'
alias lltime='ll --sort=time --reverse'
alias lltree='eza -lgha --git --git-repos -T --total-size'
alias ls='eza'
alias lt='eza --tree'

alias dig="doggo"
alias doggo-doh="doggo @https://cloudflare-dns.com/dns-query"
alias doggo-tls="doggo @tls://1.1.1.1"

alias zellij="command zellij >/dev/null 2>&1 || apk add zellij && /usr/bin/zellij"
alias git="command git >/dev/null 2>&1 || apk add git && /usr/bin/git"
alias python="command python >/dev/null 2>&1 || apk add python3 && /usr/bin/python"
alias go="command go >/dev/null 2>&1 || apk add go && /usr/bin/go"
alias nix="command nix >/dev/null 2>&1 || apk add nix && /usr/bin/nix --extra-experimental-features nix-command --extra-experimental-features flakes"

alias nvim-os76="nix run github:xenos76/os76-nvf"

# reset output on first start
clear
